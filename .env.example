POSTGRES_DB=payflow
POSTGRES_USER=payflow_dev
POSTGRES_PASSWORD=replace_with_your_own_password

# Backend running on the host. Export these before starting Spring Boot.
DB_URL=jdbc:postgresql://localhost:5432/payflow
DB_USERNAME=payflow_dev
DB_PASSWORD=replace_with_your_own_password
